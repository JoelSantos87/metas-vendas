<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Novo Usuário - Posto RM</title>
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: #f4f4f9;
      padding: 50px;
    }
    .form-container {
      max-width: 400px;
      margin: 0 auto;
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 10px rgba(0,0,0,0.2);
    }
    input, select, button {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 14px;
    }
    button {
      background-color: #0059b2;
      color: white;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }
    button:hover {
      background-color: #003d80;
    }
    a {
      display:block;
      text-align:center;
      color:#0059b2;
      text-decoration:none;
      margin-top:10px;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Cadastrar Usuário</h2>
    <form method="POST" action="/user/new">
      <input type="text" name="nome" placeholder="Nome completo" required />
      <input type="text" name="username" placeholder="Usuário" required />
      <input type="password" name="senha" placeholder="Senha" required />
      <input type="text" name="rede_id" value="P_
